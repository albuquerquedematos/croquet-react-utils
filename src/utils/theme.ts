const primary = '#ee483e'
const secondary = '#f4f4f4'
const dark = 'gray'
const light = 'lightgray'
const text = 'black'
const textDark = 'white'

const theme = {
  primary,
  secondary,
  dark,
  light,
  text,
  textDark,
  buttonPT: { root: { style: { background: primary, border: 'none' } } },
  buttonPTSecondary: { root: { style: { background: dark, border: 'none' } } }
}

export default theme